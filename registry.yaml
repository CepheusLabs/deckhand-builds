# Deckhand Builds — profile registry
#
# GENERATED. Do not hand-edit. Edit the per-printer
# `printers/<id>/profile.yaml` and regenerate via:
#
#   dart run deckhand_profile_lint --root . \
#                                  --regenerate-registry
#
# CI runs the lint without the flag and fails the build
# if any registry field has drifted from the profile.
# `latest_tag` is the one field the release pipeline
# sets independently — the generator preserves whatever
# value is already on disk for that field.

schema_version: 1

profiles:
  - id: phrozen-arco
    display_name: Phrozen Arco
    manufacturer: Phrozen
    model: Arco
    status: beta
    directory: printers/phrozen-arco
    latest_tag: null

  - id: sovol-sv08-max
    display_name: Sovol SV08 Max
    manufacturer: Sovol
    model: "SV08 Max"
    status: alpha
    directory: printers/sovol-sv08-max
    latest_tag: null

  - id: sovol-zero
    display_name: Sovol Zero
    manufacturer: Sovol
    model: Zero
    status: alpha
    directory: printers/sovol-zero
    latest_tag: null
